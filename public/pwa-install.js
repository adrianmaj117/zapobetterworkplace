(() => {
  [['meta','apple-mobile-web-app-capable','yes'],['meta','apple-mobile-web-app-status-bar-style','default'],['meta','apple-mobile-web-app-title','ZapoBetter'],['link','apple-touch-icon','/assets/daily-fruits-logo.png?v=2']].forEach(([tag,key,value]) => { const selector = tag === 'link' ? `link[rel="${key}"]` : `meta[name="${key}"]`; if (document.head.querySelector(selector)) return; const node = document.createElement(tag); if (tag === 'link') { node.rel = key; node.href = value; } else { node.name = key; node.content = value; } document.head.append(node); });
  const style = document.createElement('link'); style.rel = 'stylesheet'; style.href = '/pwa-install.css'; document.head.append(style);
  if ('serviceWorker' in navigator) window.addEventListener('load', () => navigator.serviceWorker.register('/service-worker.js').catch(() => {}));
  const standalone = window.matchMedia('(display-mode: standalone)').matches || navigator.standalone;
  const ios = /iphone|ipad|ipod/i.test(navigator.userAgent);
  if (!ios || standalone) return;
  const open = () => { const dialog = document.createElement('dialog'); dialog.className = 'ios-install-dialog'; dialog.innerHTML = '<button class="ios-install-close" aria-label="Zamknij">×</button><img src="assets/daily-fruits-logo.png" alt="Daily Fruits"><p class="eyebrow">APLIKACJA NA IPHONE</p><h2>Dodaj Magazyn do ekranu głównego</h2><ol><li>W Safari dotknij przycisku <strong>Udostępnij</strong> ⎋.</li><li>Przewiń listę i wybierz <strong>Do ekranu początkowego</strong>.</li><li>Potwierdź przyciskiem <strong>Dodaj</strong>.</li></ol><p class="ios-install-note">Aplikacja otworzy się z własną ikoną Daily Fruits i będzie działać jak zwykła aplikacja.</p><button class="button primary ios-install-done">Rozumiem</button>'; document.body.append(dialog); dialog.showModal(); dialog.querySelector('.ios-install-close').onclick = () => dialog.close(); dialog.querySelector('.ios-install-done').onclick = () => dialog.close(); dialog.addEventListener('close', () => dialog.remove()); };
  const target = document.querySelector('.login-card') || document.querySelector('.warehouse-topbar') || document.body;
  const button = document.createElement('button'); button.type = 'button'; button.className = 'ios-install-open'; button.textContent = '▣ Dodaj aplikację na iPhone'; button.onclick = open; target.append(button);
})();
